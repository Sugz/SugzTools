<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CodeDoc.Controls">

    
    <Style TargetType="{x:Type local:EditableTextBlock}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:EditableTextBlock}">
                    <Grid x:Name="PART_GridContainer"
                          Background="{TemplateBinding Background}"
                          Width="{TemplateBinding Width}"
                          Height="{TemplateBinding Height}">
                        <TextBlock x:Name="PART_TbDisplayText"
                                   Visibility="Visible"
                                   Foreground="{TemplateBinding Foreground}"
                                   Text="{Binding Text}" />
                        <TextBox x:Name="PART_TbEditText"
                                 Visibility="Collapsed"
                                 Text="{Binding Text, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:EditableTextBlock}}}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>




</ResourceDictionary>