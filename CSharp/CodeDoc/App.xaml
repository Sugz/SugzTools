<Application xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:CodeDoc.ViewModel"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:CodeDoc.Controls"
             xmlns:sgz="clr-namespace:SugzTools.Controls;assembly=SugzTools"
             xmlns:ignore="http://www.galasoft.ch/ignore"
             xmlns:Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
             x:Class="CodeDoc.App"
             StartupUri="MainWindow.xaml"
             mc:Ignorable="d ignore">

    <Application.Resources>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SugzTools;component/Resources/Colors.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!--Global View Model Locator-->
            <vm:ViewModelLocator x:Key="Locator"
                                 d:IsDataSource="True" />


            <!--GridSplitterTemplate-->
            <LinearGradientBrush x:Key="GridSplitterGradiantBrush"
                                 StartPoint="0,0"
                                 EndPoint="0,1">
                <GradientStop Color="Transparent" Offset="0" />
                <GradientStop Color="Gray" Offset="0.5" />
                <GradientStop Color="Transparent" Offset="1.0" />
            </LinearGradientBrush>
            <ControlTemplate x:Key="GridSplitterTemplate" TargetType="{x:Type GridSplitter}">
                <Border x:Name="Bd"
                        Background="Transparent" />
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="Bd" Property="Background" Value="{StaticResource GridSplitterGradiantBrush}" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>


            
            <Style x:Key="LeftPanelButtonStyle" TargetType="{x:Type sgz:SgzIcon}">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="true">
                        <Setter Property="Background" Value="{StaticResource MaxButtonMouseOver}" />
                    </Trigger>
                </Style.Triggers>
            </Style>


            <Style x:Key="OptionsButtonStyle" TargetType="{x:Type sgz:SgzCheckButton}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Height" Value="30" />
                <Setter Property="PressedBrush" Value="{StaticResource MaxBlueMouseOver}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="CornerRadius" Value="0" />
                <Setter Property="VerticalAlignment" Value="Bottom" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="true">
                        <Setter Property="Foreground" Value="White" />
                    </Trigger>
                    <Trigger Property="IsPressed" Value="true">
                        <Setter Property="Foreground" Value="White" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            
            
            <Style x:Key="ValidateButtonStyle" TargetType="{x:Type sgz:SgzIcon}">
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="false">
                        <Setter Property="Icon" Value="MdiClose" />
                        <Setter Property="Foreground" Value="{DynamicResource MaxRed}" />
                    </Trigger>
                                    
                    <Trigger Property="IsEnabled" Value="true">
                        <Setter Property="Icon" Value="MdiCheck" />
                        <Setter Property="Foreground" Value="{DynamicResource MaxBlue}" />
                    </Trigger>
                                    
                    <MultiTrigger>
                        <MultiTrigger.Conditions>
                            <Condition Property="IsEnabled" Value="true" />
                            <Condition Property="IsMouseOver" Value="true" />
                        </MultiTrigger.Conditions>
                        <Setter Property="Background" Value="{DynamicResource MaxButtonMouseOver}" />
                    </MultiTrigger>
                </Style.Triggers>
            </Style>


        </ResourceDictionary>


    </Application.Resources>

</Application>
